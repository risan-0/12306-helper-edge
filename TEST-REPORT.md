# 高铁出发测试记录

## 0.2.2 最终确认修复

重新定位可见且未遮挡的核对确认按钮，兼容隐藏的重复 ID 和核对窗口中的按钮定位。静音说明关闭后继续确认；首次点击后观察 confirmPassenger 的订单处理请求。4 秒无订单请求时重新核对并最多补点一次；已观察到请求则禁止补点，持续无响应则暂停。仅进入支付页显示完成。

15 项逻辑测试与 JavaScript 语法检查通过。本轮分批通过 9 个隔离 Edge 场景：17 席别适配、正常完整流程、演练模式、C/D 与静音偏好、静音说明在确认前/后出现、第一次确认无效后补点成功、持续无响应时暂停、已提交后刷新不重复提交。

连续运行完整浏览器套件时遇到新标签页空白/页面崩溃，未宣称全套通过；关键新增场景已分批验证。测试全部使用拦截请求的模拟页面，未登录真实账户，也未验证真实订单或节假日压力。测试可用 RAIL_TEST_FILTER 按场景名称分批运行。

## 0.2.1 修复

补齐静音车厢说明的确认步骤：先识别具体静音乘车说明并点击确定，等窗口关闭、下层按钮不再遮挡后，再点击订单核对确认。兼容勾选时弹窗、首次确认时才弹窗两种情况；后者只有在未观察到真实订单确认请求时才允许额外一次确认。不能关闭提示、按钮被遮挡或操作结果不明时暂停。

状态改为“已点击确认，等待官网处理结果”，不再将一次 DOM 点击直接描述成订单已确认。

14 项逻辑测试、22 个隔离 Edge 模拟场景通过。新增说明先于确认、说明在首次确认后出现两种流程测试，均验证静音已确认、C/D 保持、只有一次订单请求；新增已观察到订单确认请求时禁止再次确认的逻辑测试。仍未以真实账户下单验证本修复。

## 0.2.0 更新

新增 17 种席别选择，支持普通列车编号；确认弹窗默认设置 C/D 偏好和可用的静音车厢选项，再点击确认。偏好位置不足时不进行部分选座，交由官网自动分配。面板支持缩小、隐藏、从扩展弹窗重新打开，显示状态与任务运行状态分开。

13 项逻辑测试通过。浏览器模拟增加逐项检查 17 种席别的余票列与订单选项、C/D+静音在确认前生效、A/F 与静音关闭、偏好数量不足、一等座、普通车硬座、面板缩小/隐藏/重新打开不停止任务。加上既有场景，共 20 个隔离 Edge 模拟场景通过。

选座容器 #id-seat-sel、.sel-item a 和 cur 选中状态参考 12306 公开查询脚本；静音复选框按其可见说明关联定位。尚未登录真实账户验证所有车型的确认页，也没有真实出票或节假日压力测试，偏好结果最终由铁路分配。

## 0.1.2 更新

目标车次改为选填。留空时按官网列表顺序匹配所选乘车日期与精确区间的全部车次，仍要求足够人数的二等座。指定车次的优先顺序保持不变。同步更新输入校验、后台预订锁校验、运行提示和说明。

12 项逻辑测试通过；13 个隔离 Edge 模拟场景通过。新增留空和缺失车次输入、动态选中车次的后台授权、留空后自动选中首个符合条件车次并提交一次的测试。真实账户订单确认页仍未验证。

## 0.1.1 修复

根据用户真实查询页截图，修复“设置上海、目标 G1321 实际上海虹桥出发”被误报为无二等座的问题。保留精确车站匹配，站点不一致时明确显示实际车站并暂停，要求用户改为实际乘车站。另区分目标车次不存在、余票控件不可读及预订按钮不可用。没有自动扩大到同城所有车站，以免购买用户不接受的区间。

10 项逻辑测试通过（新增上述场景和分类提示回归测试）；12 个既有隔离 Edge 模拟场景重新通过。真实账户订单确认页仍未验证。

## 0.1.0 基线

测试日期：2026-09-10；浏览器集成测试完成于北京时间 14:31。

环境：Windows，Microsoft Edge 152.0.4191.66，Node.js 24.19.0，Playwright。浏览器使用独立临时用户目录，加载真实 MV3 扩展。测试使用本地模拟页面，所有测试页面 HTTPS 请求被模拟或中止。没有创建真实订单。

## 结果

8 项逻辑测试通过，12 个浏览器集成场景通过。JavaScript 语法检查通过。

### 逻辑测试

1. 车站代码、有效日期、目标车次、人数及设置参数验证。
2. 按北京时间解析定时起售，不依赖电脑所在时区。
3. 二等座人数足够才匹配，候补、无座和不足票数不匹配。
4. 精确匹配目标车次和站点代码，按填写顺序选择。
5. 订单日期、车次和区间错误或缺失时不提交。
6. 并发操作锁只允许一次预订，重建后台后锁仍生效。
7. 停止任务、其他标签页及旧任务消息不能继续操作。
8. 不能在预订前提交，不能在提交前确认。

### Edge 模拟测试

1. 完整流程：跳过二等座人数不足的首选车次，选中后续目标车次，选二等座，提交和确认各一次，到达模拟支付页。
2. 演练模式在选择乘车人和二等座后暂停，没有提交。
3. 订单区间不匹配时暂停。
4. 重名乘车人时暂停，不猜测身份。
5. 无二等座选项时暂停，不改买一等座。
6. 验证码显示时暂停，不发起查询。
7. HTTP 429 时暂停，不循环重试。
8. 查询请求未返回时点击停止，不进入预订。
9. 人工修改查询条件时暂停，扩展自动勾选不会误触发暂停。
10. 已有任务时拒绝启动第二个任务。
11. 起售计时到点后发起查询，到点前不查询。
12. 提交后刷新页面不会重复提交或确认。

## 已核对的官方公开资料

- [12306 查询页](https://kyfw.12306.cn/otn/leftTicket/init)：出发站、到达站、日期、查询按钮、车次行和二等座余票标识。
- 官方公开前端查询脚本及站名资源：3384 条站名记录；未将官网脚本复制进扩展，只按实际控件编写适配。
- [Edge 本地加载说明](https://learn.microsoft.com/en-us/microsoft-edge/extensions-chromium/getting-started/extension-sideloading)：使用 MV3 和加载解压缩扩展流程。

## 未验证的部分

- 未登录用户真实 12306 账户；真实订单确认页只能在实际登录和预订后核对。当前确认页适配依赖预期的官方数据字段和 DOM，遇到变化会暂停。
- 未完成真实出票、支付、真实排队或节假日高峰验证。
- 未验证长时间后台标签页限速、电脑休眠、不同系统时钟偏差和所有验证码变体。
- 不能保证第一时间执行、优先于其他旅客或一定抢到票。

建议在起售前用关闭“自动提交到待支付”的模式验证自己的浏览器和账户页面。演练会发起正常查询并进入预订页；不会点击提交订单。支付及验证码始终由用户在官方页面完成。
